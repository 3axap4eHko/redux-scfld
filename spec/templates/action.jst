{__info_header}
'use strict';

export default function(getState, callback) {
    return new Promise( (resolve, reject) => {
        setTimeout(() => {
            try {
                resolve(callback())
            } catch(e) {
                reject(e);
            }
        }, 1000)
    });
}
